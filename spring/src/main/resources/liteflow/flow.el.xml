<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="testChain">
        THEN(A1, A2);
    </chain>

    <chain name="test">
        WHEN(
        THEN(A1,A2,A3),
        THEN(B1,B2)
        ).ignoreError(true);
    </chain>
</flow>